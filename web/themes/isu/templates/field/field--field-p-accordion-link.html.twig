{% for item in items %}
  {% if item['content']['#title'] %}
    <a href="{{ item['content']['#url'] }}" class="button-arrow">
      <span class="material-symbols-outlined span-headline">arrow_forward</span>
      {{ item['content']['#title'] }}
    </a>
  {% endif %}
{% endfor %}
