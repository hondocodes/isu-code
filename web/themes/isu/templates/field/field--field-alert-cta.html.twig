{% for item in items %}
  {% if item['content']['#title'] %}
    <a href="{{ item['content']['#url'] }}" class="button button--light button--sm">
      {{ item['content']['#title'] }}
    </a>
  {% endif %}
{% endfor %}
